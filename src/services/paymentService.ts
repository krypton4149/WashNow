/**
 * Payment Service
 * Stripe Configuration
 */

import { STRIPE_KEY, MERCHANT_IDENTIFIER } from '../config/env';

// Stripe Publishable Key
export const STRIPE_PUBLISHABLE_KEY = STRIPE_KEY;
export { MERCHANT_IDENTIFIER };
